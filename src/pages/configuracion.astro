---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Configuración | Why Not? Perfums">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <h1 class="text-3xl font-serif text-brand-dark mb-8">Configuración de la Cuenta</h1>

        <div id="settings-content" class="bg-white shadow rounded-lg p-6">
            <h2 class="text-xl font-medium text-brand-dark mb-6">Preferencias</h2>
            <form class="space-y-6">
                <div class="flex items-center justify-between border-b border-gray-100 pb-4">
                    <div>
                        <h3 class="text-base font-medium text-gray-900">Notificaciones por Correo</h3>
                        <p class="text-sm text-gray-500">Recibe ofertas exclusivas y novedades.</p>
                    </div>
                    <div>
                        <input type="checkbox" class="h-4 w-4 rounded border-gray-300 text-brand-accent focus:ring-brand-accent" checked>
                    </div>
                </div>

                <div class="flex items-center justify-between border-b border-gray-100 pb-4">
                    <div>
                        <h3 class="text-base font-medium text-gray-900">Idioma</h3>
                        <p class="text-sm text-gray-500">Selecciona tu idioma preferido para la tienda.</p>
                    </div>
                    <div>
                         <select class="mt-1 block w-full rounded-md border-gray-300 py-2 pl-3 pr-10 text-base focus:border-brand-accent focus:outline-none focus:ring-brand-accent sm:text-sm">
                            <option>Español</option>
                            <option>English</option>
                        </select>
                    </div>
                </div>

                <div class="pt-4 flex justify-end">
                    <button type="button" class="bg-brand-dark text-white px-4 py-2 rounded text-sm font-medium hover:bg-brand-accent transition-colors">
                        Guardar Cambios
                    </button>
                </div>
            </form>
        </div>
    </div>
</Layout>
