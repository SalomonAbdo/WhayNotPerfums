---
---

<nav class="fixed w-full z-50 transition-all duration-300 bg-white/80 backdrop-blur-md border-b border-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-20">
            <!-- Logo -->
             <div class="flex-shrink-0 flex items-center">
                <a href="/" class="font-serif text-2xl font-bold tracking-tighter text-brand-dark">
                    WHY NOT? <span class="text-brand-accent text-lg italic font-light block -mt-2">perfums</span>
                </a>
            </div>

            <!-- Menú de escritorio central -->
            <div class="hidden md:flex items-center space-x-12">
                <a href="/mujer" class="text-xs tracking-[0.2em] font-medium text-brand-dark hover:text-brand-accent transition-colors uppercase">Mujer</a>
                <a href="/hombre" class="text-xs tracking-[0.2em] font-medium text-brand-dark hover:text-brand-accent transition-colors uppercase">Hombre</a>
                <a href="/unisex" class="text-xs tracking-[0.2em] font-medium text-brand-dark hover:text-brand-accent transition-colors uppercase">Unisex</a>
                <a href="/colecciones" class="text-xs tracking-[0.2em] font-medium text-brand-dark hover:text-brand-accent transition-colors uppercase">Colecciones</a>
            </div>

            <!-- Iconos de la derecha -->
            <div class="flex items-center space-x-6">
                 <!-- Búsqueda -->
                <button aria-label="Buscar" class="text-brand-dark hover:text-brand-accent transition-colors hidden">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
                        <path stroke-linecap="round" stroke-linejoin="round" d="m21 21-5.197-5.197m0 0A7.5 7.5 0 1 0 5.196 5.196a7.5 7.5 0 0 0 10.607 10.607Z" />
                    </svg>
                </button>

                <!-- Carrito -->
                <a href="/carrito" class="relative text-brand-dark hover:text-brand-accent transition-colors group">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 1 0-7.5 0v4.5m11.356-1.993 1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 0 1-1.12-1.243l1.264-12A1.125 1.125 0 0 1 5.513 7.5h12.974c.576 0 1.059.435 1.119 1.007ZM8.625 10.5a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Zm7.5 0a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Z" />
                    </svg>
                    <span id="cart-count" class="absolute -top-1 -right-2 bg-brand-dark text-white text-[10px] font-bold w-4 h-4 rounded-full flex items-center justify-center opacity-0 transition-opacity">0</span>
                </a>

                 <!-- Usuario - Menu Desplegable -->
                 <div id="user-menu-container" class="relative hidden sm:block">
                    <!-- Botón Login (visible si NO hay usuario) -->
                    <button id="login-btn" onclick="window.openAuthModal && window.openAuthModal()" aria-label="Cuenta" class="text-brand-dark hover:text-brand-accent transition-colors flex items-center">
                       <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
                           <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0ZM4.501 20.118a7.5 7.5 0 0 1 14.998 0A17.933 17.933 0 0 1 12 21.75c-2.676 0-5.216-.584-7.499-1.632Z" />
                       </svg>
                   </button>

                   <!-- Avatar Usuario (visible si SI hay usuario) -->
                   <div id="user-logged-in" class="hidden relative">
                       <button id="user-avatar-btn" class="flex items-center focus:outline-none">
                            <img id="nav-user-img" src="" alt="User" class="w-8 h-8 rounded-full border border-gray-200 object-cover hidden">
                            <span id="nav-user-placeholder" class="w-8 h-8 rounded-full bg-brand-accent/10 items-center justify-center text-brand-accent font-serif font-bold hidden border border-brand-accent/20 flex">
                               <span>U</span>
                            </span>
                       </button>

                       <!-- Dropdown Menu -->
                       <div id="user-dropdown" class="absolute right-0 mt-2 w-56 bg-white rounded-md shadow-lg py-1 ring-1 ring-black ring-opacity-5 focus:outline-none hidden transition-all duration-200 transform origin-top-right opacity-0 scale-95 z-50">
                            <div class="px-4 py-3 border-b border-gray-100 bg-gray-50/50">
                               <p class="text-sm font-medium text-brand-dark truncate" id="nav-user-name">Usuario</p>
                               <p class="text-xs text-gray-500 truncate" id="nav-user-email">email</p>
                           </div>
                           <div class="py-1">
                               <a href="/perfil" class="group flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-brand-accent">
                                   <!-- Icono Perfil -->
                                   <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4 mr-3 text-gray-400 group-hover:text-brand-accent">
                                       <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0ZM4.501 20.118a7.5 7.5 0 0 1 14.998 0A17.933 17.933 0 0 1 12 21.75c-2.676 0-5.216-.584-7.499-1.632Z" />
                                     </svg>
                                   Mi Perfil
                               </a>
                               <!-- Configuración -->
                               <a href="/configuracion" class="group flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-brand-accent">
                                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4 mr-3 text-gray-400 group-hover:text-brand-accent">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M9.594 3.94c.09-.542.56-.94 1.11-.94h2.593c.55 0 1.02.398 1.11.94l.213 1.281c.063.374.313.686.645.87.074.04.147.083.22.127.324.196.72.257 1.075.124l1.217-.456a1.125 1.125 0 0 1 1.37.49l1.296 2.247a1.125 1.125 0 0 1-.26 1.431l-1.003.827c-.293.24-.438.613-.431.992a6.759 6.759 0 0 1 0 .255c-.007.378.138.75.43.99l1.005.828c.424.35.534.954.26 1.43l-1.298 2.247a1.125 1.125 0 0 1-1.369.491l-1.217-.456c-.355-.133-.75-.072-1.076.124a6.57 6.57 0 0 1-.22.128c-.331.183-.581.495-.644.869l-.213 1.28c-.09.543-.56.941-1.11.941h-2.594c-.55 0-1.02-.398-1.11-.94l-.213-1.281c-.062-.374-.312-.686-.644-.87a6.52 6.52 0 0 1-.22-.127c-.325-.196-.72-.257-1.076-.124l-1.217.456a1.125 1.125 0 0 1-1.369-.49l-1.297-2.247a1.125 1.125 0 0 1 .26-1.431l1.004-.827c.292-.24.437-.613.43-.992a6.932 6.932 0 0 1 0-.255c.007-.378-.138-.75-.43-.99l-1.004-.828a1.125 1.125 0 0 1-.26-1.43l1.297-2.247a1.125 1.125 0 0 1 1.37-.491l1.216.456c.356.133.751.072 1.076-.124.072-.044.146-.087.22-.128.332-.183.582-.495.644-.869l.214-1.281Z" />
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
                                    </svg>
                                   Configuración
                               </a>
                               <button id="nav-logout-btn" class="w-full text-left group flex items-center px-4 py-2 text-sm text-red-600 hover:bg-red-50">
                                   <!-- Icono Logout -->
                                   <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4 mr-3 text-red-400 group-hover:text-red-500">
                                       <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0 0 13.5 3h-6a2.25 2.25 0 0 0-2.25 2.25v13.5A2.25 2.25 0 0 0 7.5 21h6a2.25 2.25 0 0 0 2.25-2.25V15M12 9l-3 3m0 0 3 3m-3-3h12.75" />
                                     </svg>
                                   Cerrar Sesión

                               </button>
                           </div>
                       </div>
                   </div>


               </div>


                <!-- Menú móvil (Hamburguesa) -->
                <button class="md:hidden text-brand-dark hover:text-brand-accent transition-colors" aria-label="Menú">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                    </svg>
                </button>
            </div>
        </div>
    </div>
</nav>

<div class="h-20"></div> <!-- Espaciador para no solapar el contenido -->

<script>
    import { auth } from '../firebase/client';
    import { onAuthStateChanged, signOut } from 'firebase/auth';
    import { cartItems } from '../store/cartStore';

    const loginBtn = document.getElementById('login-btn');
    const userLoggedIn = document.getElementById('user-logged-in');
    const userAvatarBtn = document.getElementById('user-avatar-btn');
    const userDropdown = document.getElementById('user-dropdown');
    const navUserImg = document.getElementById('nav-user-img') as HTMLImageElement;
    const navUserPlaceholder = document.getElementById('nav-user-placeholder');
    const navUserName = document.getElementById('nav-user-name');
    const navUserEmail = document.getElementById('nav-user-email');
    const navLogoutBtn = document.getElementById('nav-logout-btn');

    // Cart Logic
    const cartCount = document.getElementById('cart-count');
    cartItems.subscribe((items: any[]) => {
        if(!cartCount) return;
        const count = items.reduce((acc: number, item: any) => acc + item.quantity, 0);
        cartCount.innerText = count.toString();
        if(count > 0) cartCount.classList.remove('opacity-0');
        else cartCount.classList.add('opacity-0');
    });

    // Manejar estado de autenticación
    onAuthStateChanged(auth, (user) => {
        if (user) {
            loginBtn?.classList.add('hidden');
            userLoggedIn?.classList.remove('hidden');

            // Actualizar datos del usuario
            if (navUserName) navUserName.textContent = user.displayName || 'Usuario';
            if (navUserEmail) navUserEmail.textContent = user.email || '';

            if (user.photoURL) {
                if (navUserImg) {
                    navUserImg.src = user.photoURL;
                    navUserImg.classList.remove('hidden');
                }
                navUserPlaceholder?.classList.add('hidden');
            } else {
                navUserImg?.classList.add('hidden');
                navUserPlaceholder?.classList.remove('hidden');
                navUserPlaceholder?.classList.add('flex');
                if (navUserPlaceholder) {
                    const initial = (user.displayName || user.email || 'U')[0].toUpperCase();
                    const span = navUserPlaceholder.querySelector('span');
                    if (span) span.textContent = initial;
                }
            }
        } else {
            loginBtn?.classList.remove('hidden');
            userLoggedIn?.classList.add('hidden');
        }
    });

    // Toggle Dropdown
    userAvatarBtn?.addEventListener('click', (e) => {
        e.stopPropagation();
        const isHidden = userDropdown?.classList.contains('hidden');
        if (isHidden) {
            userDropdown?.classList.remove('hidden');
            setTimeout(() => {
                userDropdown?.classList.remove('opacity-0', 'scale-95');
                userDropdown?.classList.add('opacity-100', 'scale-100');
            }, 10);
        } else {
            closeDropdown();
        }
    });

    function closeDropdown() {
        if (!userDropdown) return;
        userDropdown.classList.remove('opacity-100', 'scale-100');
        userDropdown.classList.add('opacity-0', 'scale-95');
        setTimeout(() => {
            userDropdown.classList.add('hidden');
        }, 200);
    }

    // Cerrar dropdown al hacer clic fuera
    document.addEventListener('click', (e) => {
        if (userLoggedIn && !userLoggedIn.contains(e.target as Node)) {
            closeDropdown();
        }
    });

    // Logout
    navLogoutBtn?.addEventListener('click', async () => {
        try {
            await signOut(auth);
            window.location.href = '/';
        } catch (error) {
            console.error('Error al cerrar sesión', error);
        }
    });
</script>
